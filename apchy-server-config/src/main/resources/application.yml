server:
  port: 8888
  contextPath: /
spring:
  application:
    name: apchy-server-config
  cloud:
    config:
      label: master
      server:
        git:
          username: 996041341@qq.com
          password: Wangwb123
          searchPaths:
          uri: https://gitee.com/wwbztree/apchy-config.git
eureka:
  instance:
    #是否显示主机得IP
    perfer-ip-address: true
  client:
    service-url:
      #注册中心得地址
      defaultZone: http://localhost:8761/eureka/