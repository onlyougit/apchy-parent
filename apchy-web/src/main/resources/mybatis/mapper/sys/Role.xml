<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sptwin.apchy.web.sys.mapper.OrderMapper">

    <select id="findRolesByUserId" parameterType="java.lang.Long" resultType="com.sptwin.apchy.web.entity.Role">
        select
        r.id,r.role_name roleName,r.description,r.available
        from t_sys_user_role ur,t_sys_role r
        where ur.role_id = r.id and ur.user_id = #{value}
    </select>
</mapper>